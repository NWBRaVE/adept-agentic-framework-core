services:
  redis:
    image: redis:alpine
    container_name: agentic_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped
    networks:
      - agentic_network

volumes:
  redis_data:
    driver: local
